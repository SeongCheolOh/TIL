
public class VarEx {

	public static void main(String[] args) {
		// 첫글자가 대문자이면 객체이다.    
		// . ~안에 
		// ()함수method
		// {}함수를 처리할 내용
		// []배열
		// <>
        // ex) 덧셈해라(1,4)
        // 함수는 ~~해라, 실행해라, 동작해라 등의 의미
        // 개발자가 직접 만들기 가능, 만들어진 것 가져오기 가능
        // ''는 한 글자만, ""는 한 글자 이상
        System.out.println('글');
        System.out.println("여러글자");
        // 변수
        // a = 10; >> 10을 a에 대입하시오
        // 10 = a; 다르다!
	}

}
