package ch06.sec12;

public class Import01 {
	//같은 패키지에 있는 클래스는 아무런 조건 없이 사용 가능하지만
	//다른 패키지에 있는 클래스를 사용하려면 import문을 사용
	//동일한 패키지에 포함된 다수의 클래스를 사용해야한다면 클래스 이름을 생략하고 *를 사용할 수 있다
	//import는 하위패키지를 포함하지 않는다. 따라서 사용하고싶은 패키지를 모두 import해줘야한다
	
	
}
